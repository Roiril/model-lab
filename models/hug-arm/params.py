# CATEGORY: ロボットアーム
# hug-arm — 抱きつくロボットの片腕（肩・肘の 2 自由度）— 寸法・定数
#
# servo-arm との違い（2026-09-03。参考モデル 3 点を読んで作り直した）:
#   servo-arm は二枚の頬板でサーボを丸ごと挟んでいた。挟む構造は軸方向から
#   組めないので、横入れのために軸方向へ 5.5mm の逃げが要り、関節幅が 50.1mm
#   になっていた。参考（アームサーボ / 左腕サーボ / サーボジンバル）はどれも
#   挟んでいない。サーボのギアカバー座のまわりに軸受けリングを立て、従動側は
#   そこへ片持ちで差すだけ。軸方向にまっすぐ入るので逃げが要らない。
#   結果、腕は 9.5mm の平板が 2 枚並ぶだけになる。
#
# 使うサーボ: M5Stack Servo Kit 180° を 2 個（servo-arm と同じ実測値）。
# 360° 版は連続回転で角度を保持できないので使わない。
#
# 単位はすべて mm（viewer の数値入力に出すため。model.py で m に直す）。
# 座標（リンクのローカル系）: X = 関節軸 / Y = 腕の伸びる向き / Z = 面内の上。
# 実装上の向き: 2 本の腕は前を向いた軸のまわりに正面で開閉する（下の MOUNT_* 参照）。

# ============================================================
# サーボ実寸（M5Stack 180° = SG92R 相当。2026-08-25 実測）
# 関節の幅・軸受けの高さ・ホーン結合面はすべてここから計算する
# ============================================================
SERVO_BODY_L = 22.0            # 本体 長手（軸がずれている方向）
SERVO_BODY_W = 12.3            # 本体 幅
SERVO_BODY_H = 22.3            # 本体 高さ（底〜ケース上面・軸は含まない）
SERVO_SHAFT_OFFSET = 5.0       # 軸が本体長手中心からずれる量
SERVO_FLANGE_L = 32.2          # 羽根を含む全長
SERVO_FLANGE_W = 12.3          # 羽根の幅
SERVO_FLANGE_T = 2.2           # 羽根の厚み
SERVO_FLANGE_FROM_BOTTOM = 16.0  # 本体底〜羽根下面
SERVO_SCREW_SPACING = 28.84    # 取付ネジ穴のピッチ
SERVO_SCREW_PILOT = 1.7        # 取付ネジ下穴 ⌀（M2 セルフタップ想定）
SERVO_BOSS_DIA = 12.3          # ギアカバー座（大円柱）⌀ … これが軸受けの内側になる
SERVO_BOSS_H = 4.6             # ギアカバー座の高さ（ケース上面から）
SERVO_SHAFT_DIA = 4.6          # 出力軸スプライン ⌀
SERVO_SHAFT_H = 3.6            # スプライン高さ（ギアカバー上面〜軸先端）

# ============================================================
# サーボホーン（付属の十字ホーン。models/horn の実測値）
#
# ⚠⚠ 腕板はギアカバー座の上に直接は乗らない。中央ハブ（⌀7 × 3.6mm）が
#   スプラインにかぶさり、腕板はその上（座の上 3.6〜5.6mm）にある。
#   腕板を座の上 0〜2mm に置いた設計は実物で 3.6mm ぶつかる
#   （2026-09-04 に発覚。servo-arm は正しく「座の上 8.2mm」で組んでいた）
#
# 取り付け方: ホーンは従動側の板の「上」に載せ、腕の穴を通して上からねじで板へ
# 留める。どのサーボブラケットもこの形。板に棚も長穴も要らず、センタービスも
# 露出する。板の下穴はホーンの穴の位置に合わせるので、HORN_HOLE_R を実測する
# ============================================================
HORN_SPAN_LONG = 28.0          # 長腕の全長（端〜端）。使う穴の外側で詰める（実物 35）
                               # 詰めなくても組める。ハブの縁から 1.7mm はみ出るだけ
HORN_SPAN_SHORT = 17.0         # 短腕の全長
HORN_ARM_W_LONG = 6.8          # 長腕の幅
HORN_ARM_W_SHORT = 3.6         # 短腕の幅
HORN_HUB_DIA = 7.0             # 中央ハブ ⌀（スプラインにかぶさる部分）
HORN_T = 2.0                   # 腕板の厚み
HORN_STACK_H = 5.6             # ギアカバー座の上面 〜 ホーンの頂部（実測）
HORN_SCREW_DIA = 2.4           # センタービス ⌀
HORN_SCREW_HEAD_DIA = 4.5      # ビスの頭 ⌀（干渉確認用）
HORN_SCREW_HEAD_H = 1.5        # ビスの頭の高さ
# ⚠ 実測して入れる。長腕の穴のうち使うものの、軸中心からの距離
HORN_HOLE_R = 12.5             # 使う穴の半径。ノギスで測る（2.5mm ピッチの物が多い）
HORN_PILOT_DIA = 1.7           # 板側の下穴 ⌀（M2 セルフタップ。サーボ側と同じ値）
HORN_PILOT_DEPTH = 6.0         # 下穴の深さ

# ============================================================
# サーボの性能（載せられる重さと回せる角度を決める値）
# ============================================================
SERVO_STALL_KGCM = 1.8   # 定格ストールトルク @6.0V
SERVO_DUTY = 0.333       # 連続で効かせてよい割合。定格の 1/3 が目安
SERVO_MASS_G = 9.0       # サーボ 1 個
HORN_MASS_G = 1.5        # ホーン 1 枚
PLA_DENSITY = 0.55       # g/cm3（20% infill の実効密度）

# 0° = 腕が真横（体から離れて水平）／+ が体の前へ閉じる向き。
# 抱きつくので「閉じ切り」側を広く、開き側は体に当たる手前で止める
SHOULDER_WINDOW = (-20.0, 160.0)
ELBOW_WINDOW = (0.0, 150.0)
SERVO_SPLINE_TEETH = 20        # ホーンは 18° 刻みでしか差し替えられない

# ============================================================
# 関節モジュール（肩・肘 共通）
# 駆動側 = サーボを持つ方。従動側 = ホーンで回される方。
# 従動側は「軸受けリングに差してホーンへねじ止め」だけ。挟まない
# ============================================================
DECK_T = 4.5        # サーボを留める板の厚み。M2 セルフタップの効き代
                    # ⚠ ギアカバー座（4.6mm）より厚くすると座が板に埋まって
                    #   軸受けが成立しない。model.py が上限で頭打ちにする
JOURNAL_D = 18.0    # 軸受けリングの外径。従動側はこの外周に乗る
                    # リングはホーンの腕板の直下まで伸ばす（座の上 8.0mm）。
                    # 腕板の下は中央ハブ ⌀7 しか無いので、リングは干渉しない
JOURNAL_CLR = 0.30  # 軸受けの片側すきま。回る面
                    # FDM の縦穴は 0.1〜0.2 小さく出るので、実効は 0.2 前後になる。
                    # 渋ければ 0.4 へ。傾きの遊びは (すきま x2) / かかり長さ
BOSS_CLR = 0.3      # リング内径 ↔ ギアカバー座 の片側すきま
HORN_CLR = 0.2      # ホーンの腕板 ↔ 板の表面 のすきま
HUB_WALL = 2.5      # 下穴のまわりに残す肉 → ハブ半径 = 下穴の半径 + これ
SERVO_CLR = 0.4     # サーボはめ込みクリアランス（片側）

# ============================================================
# リンク（腕）
# ⚠ 長さは重さより効く。servo-arm の実測では 100+100mm で肩トルクの 54% を
#   自重が食っていた。抱きつくだけで物は持たないので、短く細くする
#
# 桁は一定幅・一定厚み。参考（アームサーボ / サーボジンバル）の腕はどれも
# 一定幅の板で、丸穴を並べたものは 1 つも無い。抜くときはスポークや角窓の
# ような大きな構造開口で抜いている
# ============================================================
L1 = 78.0           # 肩軸 → 肘軸
L2 = 90.0           # 肘軸 → 腕の先端
PLATE_T = 7.0       # 平板の厚み（X）… model.py がホーンの腕板の高さから決め直す
BEAM_W = 20.0       # 桁の幅（Z）。全長で一定
                    # ⚠ 肘のサーボ穴（13.1mm）の両脇に肉を残す幅が要る
BEAM_TAPER = 0.20   # ハブから桁幅へ絞る区間（リンク長に対する割合）

# 前腕は内側へ曲げる。まっすぐだと点でしか当たらず、抱きつく形にならない
FORE_CURL = 55.0    # 前腕全体の曲がり（度）。+ が抱き込む向き
FORE_NODES = 5      # 曲げを近似する節点の数

# 肘サーボの配線の通り道（上腕の裏面に彫る溝）
# ⚠ 結束バンドは使えない。バンドは桁を一周して表裏に 1.5mm ずつ出るが、
#   上腕の表面は前腕が、裏面はブラケットが、どちらも 0.4mm で通る。
#   溝に沈めて、ブラケットの掃く範囲（肩から R 33mm）の外で体へ抜く。
#   溝の中はホットボンドかテープで留める
CABLE_GROOVE = 1       # 1 で彫る
CABLE_GROOVE_W = 4.0   # 幅（3 芯リボン 4.5 x 1.3 を縦にして通す）
CABLE_GROOVE_D = 2.0   # 深さ（配線 1.5 が沈む）
CABLE_EXIT = 36.0      # 肩軸からここで溝を終え、体の板の穴へ抜く（ブラケット R33 の外）

# 桁の中抜き。面内の曲げでは中央の肉は効かないので、真ん中を 1 本だけ抜く。
# 配線と結束バンドの通り道も兼ねる
SLOT = 1            # 1 で抜く
SLOT_RAIL = 4.5     # 両脇に残す桁（片側）
SLOT_MARGIN = 4.0   # ハブ（や先端）の縁から溝までの距離
SLOT_MIN = 35.0     # これより短くしか取れないなら抜かない
                    # 上腕は両端のハブと肘のサーボ座でほぼ埋まっていて 27mm しか
                    # 取れない。短い溝を無理に入れるのは理由の無い穴なので開けない

# ============================================================
# 当たり面（TPU パッド）
# 硬い PLA のまま抱きつくと、サーボは 1.8kgf·cm を出し続けるので相手に食い込む。
# 前腕の内側の縁に TPU の袖をかぶせて、当たりを面で受けて逃がす
# ============================================================
PAD = 1             # 1 でパッドを作る
PAD_T = 2.4         # パッドの肉厚（厚くすると先端で効くので重くなる）
PAD_GRIP = 1.2      # 袖が桁を掴む深さ（片側）
PAD_CLR = 0.15      # 袖の片側すきま（TPU なので詰める）
PAD_FROM = 0.45     # パッドを載せ始める位置（前腕の長さに対する割合）

# ============================================================
# 肩ブラケット（体側への取付）
# ⚠ 肘サーボの本体は上腕の裏へ 14.7mm 出る。肩を 90° 以上回すと肘が体の前を
#   通るので、体の板はそれより後ろに要る。ブラケットを厚くして稼ぐ。
#   薄くしたければ肩の可動域を 95° までに制限する（ソフトの制限に頼ることになる）
# ============================================================
BRK_T = 18.0        # ブラケットの厚み。model.py が必要な最小値を出して警告する
BRK_MARGIN = 5.0    # サーボまわりに残す縁
WIRE_RELIEF = 4.5   # 羽根の座ぐりを尻尾側へ伸ばす量。配線は本体の底近くから尻尾側へ
                    # 出るので、座ぐりの端壁に押しつけないための逃げ
BRK_HOLE_DIA = 3.4  # 取付穴 ⌀（M3 ばか穴）。表から M3 x 20 で体の板へ（板 3mm + ナット）
BRK_CBORE_DIA = 6.2 # 取付ねじの頭を沈める座ぐり ⌀。表面を上腕が 0.4mm で通るので、
BRK_CBORE_H = 3.2   # 頭が出ていると必ず当たる
BRK_HOLE_PITCH = 30.0  # 取付穴のピッチ（Z）

# ============================================================
# 体への据え付け（プレビュー用。左右 2 本の位置関係を決める）
# ============================================================
SHOULDER_SPAN = 96.0   # 左右の肩軸の間隔（体の幅）
# ⚠ 腕は肩軸まわりに正面で開閉する。左右を同じ深さに付けると、閉じたとき同じ
#   平面で交差してぶつかる（実測で 105mm 重なった）。人が抱くときと同じで、
#   片方を前にずらして通す。前腕の板厚 9.5mm ＋ 逃げより大きくすること
HUG_OFFSET_Y = 17.0    # 右腕を左腕より前に出す量。0 にすると必ずぶつかる
MOUNT_TILT = 0.0       # 肩軸を前下がりに傾ける角度
                       # ⚠ 0 以外にすると左右の振れる面が平行でなくなり、
                       #   前後にずらしても中央で当たることがある（下の [pair] で確認）
SHOW_PAIR = 1          # 1 でプレビューに反対側の腕も出す

# ============================================================
# 姿勢（プレビュー用。印刷用 STL は各パーツ単独の向きで書き出す）
# ============================================================
POSE_SHOULDER = 75.0   # 肩の角度（0=真横に水平, +で前へ閉じる）
POSE_ELBOW = 80.0      # 肘の角度（上腕からの相対角）
SHOW_SERVO = 1         # サーボとホーンのダミーを重ねる（1=表示）

# --- ブラウザUIからのパラメータ上書き ---
try:
    from param_override import apply_overrides
    apply_overrides(globals())
except Exception:
    pass
