"""facet-bot — 曲げないで「丸い胴体」を作る多面体(ファセット)ロボ（アクリル可）。

curve-bot(リビングヒンジ)のアクリル代替。曲がる背面を M枚の平らなファセットに分割し、
前面フラット(顔)＋各ファセットを D字ポリゴンのフタにタブ差し込み＆接着で組む。曲げゼロ。
単位 mm。
"""

D_OUT = 70.0     # 胴体の外径（多角形の外接円 ≒）
T = 3.0          # 板厚（アクリル 3mm）
H = 60.0         # 高さ

WF = 44.0        # 前面フラット幅（顔パネル＝弦）。D_OUT-T の直径 67 未満であること
M_ARC = 8        # 背面アーチを何枚のファセットに割るか（多い＝丸い/部品増）

# タブ（各ファセット上下端 → フタのスロット）
TAB_W = 5.0      # タブ接線幅
TAB_PER_22MM = 22.0   # この幅ごとにタブ1本（ファセット幅に応じ本数自動）

# 顔（前面パネルに彫刻）
FACE = (1, 3)    # face-sheet のセル（下段3＝まんまる目＋笑み）
FACE_W = 38.0    # 顔の目標幅（前面パネル WF=44 に収める）

RIM = 2.0        # フタがファセット外面より張り出す量（スロット外縁との橋）
GAP = 8.0        # シート内パーツ間ギャップ
