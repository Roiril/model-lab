# 卓上ランプ型ロボットアーム・スタンド — 寸法定数 (単位: m)
# v1: 形（シルエット）確定フェーズ。分割面・圧入ペグは v2 で追加する。
#
# 搭載予定ハンド: Hiwonder / LewanSoul 5本指バイオニックロボットハンド（右手）
#   → ヘッドは現状プレースホルダ。実機の取付寸法が分かり次第マウントを合わせる。

import math

# ============================================================
# 全体目標サイズ（卓上小型）
# ============================================================
TARGET_BASE_L = 0.106   # ベース最大長(前後X)
TARGET_HEIGHT = 0.280   # 全高目安（ヘッド込み）

# ============================================================
# ベース（平たい D字/盾型トレー：丸いリム＋中央の窪み）
# ============================================================
# 上面視の外周輪郭（X前方, Y左右）。前広・後ろすぼまりの丸D字。
# 支柱根元 = 原点(0,0) は後方寄り（後縁から約 1/3）に来るよう前方へ寄せて配置。
BASE_OUTLINE = [
    (0.072, 0.000),
    (0.052, 0.041),
    (0.012, 0.045),
    (-0.020, 0.029),
    (-0.034, 0.000),
    (-0.020, -0.029),
    (0.012, -0.045),
    (0.052, -0.041),
]
BASE_H = 0.016        # ベース全高 16mm（平たい）
RIM_FLOOR = 0.006     # 中央窪みの床高さ 6mm（= 窪み深さ 10mm）
RIM_SCALE = 0.72      # リム土手の幅を決める内側輪郭の縮小率
RIM_CENTER_X = 0.019  # 輪郭の重心X（縮小の中心）
BASE_BEVEL_W = 0.005  # 外周エッジの丸め幅（ピロー状の土手）
BASE_BEVEL_SEG = 4

# ============================================================
# 支柱（スワンネック：根元フレア → 上で前方へ反る、断面はやや扁平）
# ============================================================
# 中心線の制御点 (x前方, z上) と 半径。XZ平面内。根元を原点に置く。
COL_PTS = [
    # (x,      z),      radius
    ((0.000, 0.004), 0.0150),  # フレアの足：ベース窪み床に埋まり裾広がり
    ((0.000, 0.020), 0.0108),  # 立ち上がり
    ((-0.004, 0.085), 0.0090),  # 下部：わずかに後傾して溜め
    ((0.012, 0.170), 0.0072),  # 中上部：前方へ反り始め
    ((0.034, 0.236), 0.0058),  # 首：前方へ大きく反る 約12mm径
]
COL_BEVEL_RES = 12    # 断面の丸さ
COL_FLATTEN_Y = 0.80  # 左右(Y)を縮めて扁平ブレード状に

# ============================================================
# ヘッド（関節クラスタ＝プレースホルダ）
# ============================================================
HEAD_KNUCKLE_R = 0.010   # 手首ピボットを模した横向き円柱の半径
HEAD_KNUCKLE_L = 0.026   # その長さ(Y方向)
HEAD_PLATE_W = 0.030     # マウントプレート 横(Y)
HEAD_PLATE_L = 0.030     # マウントプレート 前後
HEAD_PLATE_T = 0.005     # プレート厚
HEAD_FWD = 0.020         # ナックル→プレート中心の前方オフセット
HEAD_UP = 0.014          # 同 上方オフセット
HEAD_TILT_DEG = 35       # プレート前傾角
