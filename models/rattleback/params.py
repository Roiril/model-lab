"""rattleback (celt) パラメータ。単位: m

一方向にしか滑らかに回らない物体。逆回しすると振動して停止し、
ひとりでに反対方向に回り始める。秘密は「曲率主軸」と「慣性主軸」の微小なズレ。
"""

# 半楕円体の本体
A = 0.060     # 60mm 長軸 (x)
B = 0.020     # 20mm 短軸 (y)
C = 0.008     # 8mm  高さ (z: 底面 z=-C ～ 上面 z=0)

# 非対称化用の対角質量コブ（上面に 2 つ）
# これが慣性主軸を幾何主軸から回転させる
BUMP_R = 0.005       # 5mm 半径
BUMP_H = 0.006       # 6mm 高さ
BUMP_OFFSET_X = 0.018  # x 方向オフセット (±)
BUMP_OFFSET_Y = 0.006  # y 方向オフセット (逆符号)

# メッシュ解像度
N_LON = 96   # 経度分割
N_LAT = 48   # 緯度分割（底面半分だけ使う）
