# -*- coding: utf-8 -*-
"""ガイスター (Geister) おばけコマの寸法・配色（単位: m）。

ボードゲーム「ガイスター」の駒（おばけ）を Unity 表示用 GLB として再構築する。
弾丸型（丸い頭→下に広がる裾）の白い本体。正面に目の穴2つ、
裏面に赤/青の丸マーカーと縦のひだ（シーツの襞）＋なみなみの裾。

寸法は実物写真（正面・側面・裏の3枚）から比率を採寸。高さ実測 ≈ 50mm。
断面は楕円（奥行き < 幅）。
"""

# --- 全体寸法 -------------------------------------------------------------
HEIGHT = 0.050        # 50mm 高さ（実測）
RX_BASE = 0.012       # 底の左右half幅 12mm（幅 ≈ 24mm）
DEPTH_RATIO = 1.00    # 奥行き/幅（1.0=円形断面。側面も正面と同じ丸み）

# 本体メッシュ解像度
N_THETA = 96          # 周方向分割
N_Z = 130             # 高さ方向分割

# 正面シルエットの half幅プロファイルを数式生成（滑らかな1本の曲線）。
# 下部＝ベル状に広がる裾、上部＝丸くブラントなドーム頭。
SHOULDER_T = 0.72     # 肩（胴からドームへ移る高さ t）
SHOULDER_REL = 0.90   # 肩での half幅（RX_BASE 比）
BODY_FLARE_POW = 1.7  # 裾の広がり具合（大=裾で急に広がる、小=直線的）
DOME_POW = 2.4        # 頭の丸み（2=半楕円/やや尖り, >2=ブラントで丸い）
FLARE_FOOT = 0.055    # 最下部でスカート状に外へ広がる量（RX_BASE 比）
FOOT_WIDTH = 0.13     # フレアが効く高さ幅 t（大=なだらか）

# --- 目（正面の凹み穴・貫通せず暗く見せる）-------------------------------
EYE_T = 0.79          # 高さ位置（頭のやや下）
EYE_DX = 0.0032       # 中心から左右 ±3.2mm
EYE_R = 0.0016        # 半径 1.6mm
EYE_DEPTH = 0.0030    # 掘り込み深さ 3mm

# --- 裏マーカー（赤/青の丸。小さめ・埋め込み）----------------------------
MARK_T = 0.48         # 高さ位置（中央よりやや下）
MARK_R = 0.0026       # 半径 2.6mm（直径 ≈ 5.2mm。小さめ）
MARK_POCKET = 0.0006  # 平らな座ぐり深さ 0.6mm
MARK_INSET = 0.0003   # 表面から 0.3mm 沈めて出っ張らせない

# --- 裏のひだ（縦の溝。底面まで到達・底面は平ら）------------------------
FLUTE_N = 2           # 溝の本数
FLUTE_SPREAD = 0.19   # 溝の左右位置（RX_BASE 比。中央に寄せる）
FLUTE_TOP = 0.432     # 溝の上端 t（マーカー下端ぎりぎり）
FLUTE_R = 0.0016      # 溝カッター半径 1.6mm（細め）
FLUTE_TAPER = 0.006   # 上端テーパー長 6mm（先端をマーカー直下まで詰める）
FLUTE_TIP_R = 0.0005  # 先端の丸め半径 0.5mm（尖りすぎない）

# --- 配色（sRGB 0-255）---------------------------------------------------
BODY_WHITE = (238, 238, 235)   # 白樹脂本体
EYE_DARK   = (40, 40, 42)       # 目の穴（暗色）
MARK_BLUE  = (24, 96, 176)      # 青マーカー
MARK_RED   = (198, 42, 40)      # 赤マーカー

VARIANTS = {
    "blue": MARK_BLUE,
    "red": MARK_RED,
}
