# -*- coding: utf-8 -*-
"""ガイスター (Geister) おばけコマの寸法・配色（単位: m）。

ボードゲーム「ガイスター」の駒（おばけ）を Unity 表示用 GLB として再構築する。
弾丸型（丸い頭→下に広がる裾）の白い本体。正面に目の穴2つ、
裏面に赤/青の丸マーカーと縦のひだ（シーツの襞）＋なみなみの裾。

寸法は実物写真（正面・側面・裏の3枚）から比率を採寸。高さ実測 ≈ 50mm。
断面は楕円（奥行き < 幅）。
"""

# --- 全体寸法 -------------------------------------------------------------
HEIGHT = 0.050        # 50mm 高さ（実測）
RX_BASE = 0.012       # 底の左右half幅 12mm（幅 ≈ 24mm）
DEPTH_RATIO = 1.00    # 奥行き/幅（1.0=円形断面。側面も正面と同じ丸み）

# 本体メッシュ解像度
N_THETA = 96          # 周方向分割
N_Z = 120             # 高さ方向分割

# 正面シルエットの half幅プロファイル（t: 0=底, 1=頭頂）→ RX_BASE 倍
# 底は最大幅、緩やかに絞りながら上昇し、上部でドーム状に丸めて頭頂へ収束。
PROFILE = [
    (0.00, 1.00),
    (0.15, 0.985),
    (0.30, 0.960),
    (0.45, 0.925),
    (0.55, 0.893),
    (0.65, 0.850),
    (0.72, 0.800),
    (0.80, 0.710),
    (0.87, 0.570),
    (0.92, 0.440),
    (0.96, 0.300),
    (0.985, 0.160),
    (1.00, 0.000),
]

# --- 目（正面の凹み穴・貫通せず暗く見せる）-------------------------------
EYE_T = 0.79          # 高さ位置（頭のやや下）
EYE_DX = 0.0032       # 中心から左右 ±3.2mm
EYE_R = 0.0016        # 半径 1.6mm
EYE_DEPTH = 0.0030    # 掘り込み深さ 3mm

# --- 裏マーカー（赤/青の丸）----------------------------------------------
MARK_T = 0.50         # 高さ位置（中央）
MARK_R = 0.0032       # 半径 3.2mm（直径 ≈ 6.4mm）
MARK_INSET = 0.0004   # 表面から 0.4mm 沈める

# --- 裏のひだ（縦の溝。底面まで到達・底面は平ら）------------------------
FLUTE_N = 4           # 溝の本数（裏の下半分）
FLUTE_TOP = 0.52      # 溝の上端 t
FLUTE_R = 0.0026      # 溝カッター半径 2.6mm

# --- 配色（sRGB 0-255）---------------------------------------------------
BODY_WHITE = (238, 238, 235)   # 白樹脂本体
EYE_DARK   = (40, 40, 42)       # 目の穴（暗色）
MARK_BLUE  = (24, 96, 176)      # 青マーカー
MARK_RED   = (198, 42, 40)      # 赤マーカー

VARIANTS = {
    "blue": MARK_BLUE,
    "red": MARK_RED,
}
