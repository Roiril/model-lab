"""laser-bot — タブ&スロット箱組みのかわいいロボット（3mm アクリル / 静的）。

単位は mm（laser_core / SVG は mm 系。bpy パイプラインの m とは別系統）。
"""

# 外形（やや背高のずんぐり体型でかわいく）
W = 60.0   # 幅 (x)
D = 52.0   # 奥行 (y)
H = 64.0   # 高さ (z)

# 素材・嵌合は laser_core の単一真実源を使う（MAT_T / KERF）
# ここで上書きしたい場合のみ定義する。

# 顔（front パネル W x H のローカル座標, y は上→下＝SVG 系）
EYE_R = 7.0          # 目の半径
EYE_Y = 0.40         # 目の縦位置（H 比, 上から）
EYE_DX = 0.20        # 目の左右オフセット（W 比, 中心から）
MOUTH_W = 16.0       # 口の横幅
MOUTH_TH = 2.4       # 口の帯の太さ
MOUTH_DIP = 5.0      # 口の笑みの曲がり（下に膨らむ量）
MOUTH_Y = 0.66       # 口の縦位置（H 比, 上から）
CHEEK_R = 4.0        # ほっぺの半径
CHEEK_Y = 0.58       # ほっぺ縦位置
CHEEK_DX = 0.38      # ほっぺ左右（W 比）

# シート内のパネル間ギャップ
GAP = 6.0
