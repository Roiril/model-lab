"""
wobbler: ギリギリ自立する"変な塔"。
- 底面は平面、上に向かって細くなる（FDMサポート無しで印刷可）
- 断面は回転する星型 + コブ
- 局所的な半径変化の dr/dz は 1.0 未満に収める（45°オーバーハング制約）
単位: メートル。mm値はコメント併記。
"""

# 全体高さ
HEIGHT = 0.080  # 80mm

# 半径プロファイル（テーパー）
R_BASE = 0.022  # 22mm 底面半径
R_TOP = 0.006   # 6mm 頂点半径

# 星型断面
LOBE_COUNT = 5       # 花弁数
LOBE_AMP = 0.18      # R に対する振幅比

# ねじれ（全高あたりの総回転数）
TWIST_TURNS = 1.0

# コブ（軸方向のうねり）
BUMP_FREQ = 5        # 全高あたりの波数
BUMP_AMP = 0.07      # R に対する振幅比

# 周方向の追加うねり
RIPPLE_FREQ_J = 3
RIPPLE_AMP = 0.035

# オーバーハング安全マージン
# 実効 dr/dz がこの値を超えないよう設計時に確認する
MAX_OVERHANG_SLOPE = 0.95   # tan(45°)=1.0 に対しマージン

# メッシュ解像度
N_STEPS = 240   # 軸方向スライス数
N_CROSS = 96    # 断面頂点数

# プリントメタ
MIN_FEATURE_MM = 1.5
