"""curve-bot — リビングヒンジで巻く円筒カプセル型ロボット（3mm MDF/合板 推奨）。

単位 mm。帯(band)を丸めて筒にし、両端に丸フタ(cap)をタブ差しする。前面に顔を彫刻。
アクリルはヒンジで割れやすいので、この子は MDF/シナ合板 2.5-3mm 推奨。
"""

D_OUT = 62.0     # 円筒の外径
T = 3.0          # 板厚（MDF）
H = 60.0         # 高さ（円筒の軸方向）

# タブ（帯の上下端→フタのスロットへ）
N_TAB = 22       # タブ本数（＝フタのスロット数）。多いほど円がなめらか
TAB_W = 4.5      # タブの接線幅
# タブ高さ = 板厚 T（フタ厚を貫通して面一）

# リビングヒンジ（帯を曲げるスリット）
HINGE_COL = 3.0      # 列間隔（曲げ方向）
HINGE_SEG = 10.0     # 縦スリット1本の長さ
HINGE_LIG = 3.5      # ブリッジ(ligament)＝撓むつなぎ
HINGE_VMARGIN = 0.0  # 0=スリットが上下端まで開口（連続レールが残ると曲がらない）
END_MARGIN = 7.0     # 帯の左右端（継ぎ目）を切らない余白

# 顔（front＝帯の中央 u=C/2 に置く。剛性を残すため窓部はヒンジを抜く）
FACE = (1, 3)        # face-sheet のセル（下段3＝まんまる目＋笑み）
FACE_W = 46.0        # 顔の目標幅
FACE_WINDOW_HALF = 27.0  # 顔窓の半幅（この u 範囲はヒンジを生成しない）

GAP = 8.0            # シート内パーツ間ギャップ
