# CATEGORY: かわいいロボット
# square-bot — しかくっぽい1サーボ・キャラ（パン）寸法・定数（単位: m）
#
# 機構は lib/servo_core.py に集約（round-bot と完全共有）。
# ここは「外皮（角丸ボックス胴＋角丸頭＋点目）」と調整スライダーだけ。
# 数値はすべてメートル。コメント `# ラベル` がブラウザUIのスライダー名になる。

import math  # noqa: F401

# --- 全体サイズ ---
# 頭は回転時にサーボ上部ケースの丸逃げ（半径≈17.5mm）を内包する必要があるため
# 幅・奥行きはそれ以上を確保する。
BODY_W = 0.044    # 胴の幅（X）
BODY_D = 0.040    # 胴の奥行き（Y）
BODY_H = 0.044    # 胴の高さ
BODY_FILLET = 0.006  # 胴の角丸半径（四隅）
HEAD_W = 0.044    # 頭の幅（X）
HEAD_D = 0.040    # 頭の奥行き（Y）
HEAD_H = 0.030    # 頭の高さ
HEAD_FILLET = 0.006  # 頭の角丸半径
WALL = 0.002      # 胴の肉厚
DECK_T = 0.003    # デッキ厚（サーボを受ける天板）

# --- 目（前面 +Y のくぼみ）---
EYE_R = 0.0034    # 目の大きさ（半径）
EYE_DX = 0.010    # 目の左右間隔（中心から）
EYE_H = 0.015     # 目の高さ（接合面から）
EYE_DEPTH = 0.002 # 目のくぼみ深さ

# --- サーボ結合まわり（高さはサーボ実寸から自動計算。ここは微調整用）---
SERVO_SCREWS = 0      # サーボ固定ねじ穴を開ける（1=開ける / 0=無ねじ・頭で挟む）
SERVO_CLR = 0.0004    # サーボはめ込みクリアランス
COUPLING_GAP = 0.000  # ホーン結合面の微調整（0=ギアカバー座の真上＝ホーン実位置）

# --- 配線出口（背面 -Y の下端）---
WIRE_W = 0.006        # 配線スロットの幅
WIRE_H = 0.007        # 配線スロットの高さ（底から）

# --- ブラウザUIからのパラメータ上書き ---
try:
    from param_override import apply_overrides
    apply_overrides(globals())
except Exception:
    pass
