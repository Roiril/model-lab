# CATEGORY: ロボットアーム
# servo-arm — 肩・肘にサーボを 1 個ずつ置いた片腕（2 自由度）— 寸法・定数
#
# 使うサーボ: M5Stack Servo Kit 180° を 2 個（2026-08-25 に実物を採寸し、下の
# SERVO_* / HORN_* がそのまま合うことを確認済み）。同社の 360° 版は連続回転
# （PWM のデューティで速度と向きを決める）で角度を保持できないので使わない。
#
# 単位はすべて mm（viewer の数値入力に出すため。model.py で m に直す）。
# 座標: X = 関節軸の向き（左右）, Y = 腕の伸びる向き, Z = 上。
# 肩・肘は同じ「関節モジュール」で、寸法はすべてここから引く。
# サーボ・ホーンの寸法もここにあるので、別のサーボへ替えるときはこのブロックだけ直す。

# ============================================================
# サーボ実寸（既定 = SG92R。lib/servo_core.py の SG92R と同値）
# 別のサーボにするときはここを実測値で置き換える。関節の幅・逃げ・
# ホーン結合面の高さは全部これから計算するので、他はさわらなくてよい。
# ============================================================
SERVO_BODY_L = 22.0            # 本体 長手（軸がずれている方向）
SERVO_BODY_W = 12.3            # 本体 幅
SERVO_BODY_H = 22.3            # 本体 高さ（底〜ケース上面・軸は含まない）
SERVO_SHAFT_OFFSET = 5.0       # 軸が本体長手中心からずれる量
SERVO_FLANGE_L = 32.2          # 羽根を含む全長
SERVO_FLANGE_W = 12.3          # 羽根の幅
SERVO_FLANGE_T = 2.2           # 羽根の厚み
SERVO_FLANGE_FROM_BOTTOM = 16.0  # 本体底〜羽根下面
SERVO_SCREW_SPACING = 28.84    # 取付ネジ穴のピッチ
SERVO_SCREW_PILOT = 1.7        # 取付ネジ下穴 ⌀（M2 セルフタップ想定）
SERVO_BOSS_DIA = 12.3          # ギアカバー座（大円柱）⌀
SERVO_BOSS_H = 4.6             # ギアカバー座の高さ（ケース上面から）
SERVO_SHAFT_DIA = 4.6          # 出力軸スプライン ⌀
SERVO_SHAFT_H = 3.6            # スプライン高さ（ギアカバー上面〜軸先端）
SERVO_TAB_HOLE = 3.0           # 羽根の穴 ⌀（ダミー表示用）

# ============================================================
# サーボホーン（既定 = SG92R 付属の十字ホーン）
# 長腕がアームの伸びる向きに寝る。受け溝はこの寸法で彫る。
# ============================================================
HORN_SPAN_LONG = 35.0          # 長腕の全長（端〜端）
HORN_SPAN_SHORT = 17.0         # 短腕の全長
HORN_ARM_W_LONG = 6.8          # 長腕の幅
HORN_ARM_W_SHORT = 3.6         # 短腕の幅
HORN_HUB_DIA = 7.0             # 中央ハブ ⌀
HORN_T = 2.0                   # 腕板の厚み（＝受け溝の深さ）
HORN_SCREW_DIA = 2.4           # センタービス ⌀（軸に締めるビス）
HORN_CBORE_DIA = 5.5           # そのビス頭のザグリ ⌀
HORN_CBORE_LEDGE = 1.2         # ザグリ底に残す肉厚

# ホーン腕へ効かせる追加ビス（0 で無し）。実物のホーンは穴ピッチが個体で違うので
# 長穴にしてある。HORN_ARM_SLOT の範囲内なら、どの穴を使っても合う
HORN_ARM_SCREW_N = 2           # 本数（長腕の両側へ 1 本ずつ）
HORN_ARM_SCREW_R = 12.0        # 軸中心から長穴の中央までの距離
HORN_ARM_SCREW_DIA = 1.9       # 長穴の幅 ⌀（M2 セルフタップ）
HORN_ARM_SLOT = 7.0            # 長穴の長さ（腕の向き）… R±3.5mm のどの穴でも通る

# ============================================================
# サーボの性能（形ではなく、載せられる重さと回せる角度を決める値）
# ============================================================
SERVO_STALL_KGCM = 1.8   # 定格ストールトルク @6.0V（4.8V なら 1.6）
SERVO_DUTY = 0.333       # 連続で効かせてよい割合。定格の 1/3 が目安
SERVO_MASS_G = 9.0       # サーボ 1 個
HORN_MASS_G = 1.5        # ホーン 1 枚
PLA_DENSITY = 0.55       # g/cm3（20% infill の実効密度）。質量とトルクの見積り用

# 電気的な可動域は 180°。機械的にはそれより回る（check_arm.py の実測で
# 肩 -25°〜+180° = 205° / 肘 -30°〜+180° = 210°）ので、どの 180° を使うか決める。
# 0° = 腕が前へ水平・+ が上向き。卓上の物を取れるよう、下へ振れる側を残した
SHOULDER_WINDOW = (-25.0, 155.0)
ELBOW_WINDOW = (-30.0, 150.0)

# スプライン 20 山 ＝ ホーンは 18° 刻みでしか差し替えられない。組むときは
# サーボを中央（90°）にしてから腕を窓の中央へ当てて押し込む（ずれは最大 ±9°）
SERVO_SPLINE_TEETH = 20

# ============================================================
# 関節モジュール（肩・肘 共通）
# サーボは駆動側の板（デッキ）に吊り下げ、従動側は「ホーン受けの頬板」と
# 「反対側のピン受けの頬板」でサーボを挟む二点支持にする。
# ============================================================
DECK_T = 7.0          # サーボを留める板の厚み（羽根座を彫った残りがネジの効き代 4.5mm）
PIVOT_T = 4.0         # 反ホーン側・駆動側の腕の厚み（ピンが生える板）
CHEEK_A_T = 3.5       # 反ホーン側・従動側の頬板の厚み（ピンが刺さる板）
CHEEK_B_T = 6.0       # ホーン側・従動側の頬板の厚み（溝の上に残る肉）
GAP_ROT = 0.6         # 回る面どうしの軸方向すきま
GAP_SERVO = 5.5       # サーボ本体底 ↔ 従動頬板 のすきま
                      # ⚠ ここは「組み立ての逃げ」。リンクは横から差し込むので、
                      # 頬板がピン先端を、ホーンがスプラインを越える高さまで
                      # 持ち上げられる必要がある。足りないと物理的に組めない
                      # （小さく書いても必要量まで自動で広がる）
SERVO_CLR = 0.4       # サーボはめ込みクリアランス（片側）

PIN_DIA = 8.0         # ピボットピン ⌀（駆動側から生えて従動頬板を受ける）
PIN_CLR = 0.30        # ピン穴の片側すきま
PIN_TAPER = 0.5       # ピン先端の細り（差し込みの誘い＋横向き印刷の垂れ吸収）

R_HUB = 22.0          # 従動側 頬板の半径（ホーン長腕が収まる大きさへ自動補正）
R_WEB = 19.0          # 頬板どうしをつなぐ背板の内半径（サーボ上部の回転逃げ）
WEB_T = 3.0           # 背板の厚み（半径方向）… R_WEB + WEB_T が背板の外半径
SPINE_IN = 24.0       # 駆動側 支柱の内半径（従動側の回転を逃がす）
DECK_FRONT_R = 14.0   # デッキの前側（腕が伸びる側）の張り出し
DECK_BACK = 29.0      # デッキの後ろ側（サーボ尻尾・支柱側）の長さ
DECK_W = 17.5         # デッキ・支柱の幅（梁と底面を揃えるため梁の高さまで自動で広がる）

# ============================================================
# リンク（腕）
# ============================================================
LINK = 100.0          # 関節から関節まで（肘から手先フランジ外面までも同じ）
BEAM_W = 8.0          # 腕の梁の幅（X）… 関節の幅より狭くてよい（軽くするため）
                      # 何かを持たせず軽く抱きつくだけなので細くしてある。
                      # 肉抜き穴が梁を貫くので、断面は上下の桟（各 BEAM_WALL）と
                      # 穴どうしの間の縦リブから成るはしご状になる
BEAM_H = 18.0         # 腕の梁の高さ（Z）… ホーン短腕が入る高さまで自動で上がる
BEAM_HOLE = 1         # 梁に肉抜き穴を開ける（1=開ける）。軽くなる＋結束バンドを通せる
BEAM_WALL = 3.5       # 肉抜き穴のまわりに残す肉

# 手先フランジ（前腕の先端。ハンドや 3 個目の関節を後付けする面）
TIP_T = 4.0           # フランジの厚み
TIP_W = 24.0          # フランジの幅（X）
TIP_H = 22.0          # フランジの高さ（Z）… 梁と同じにすると底が平らのまま刷れる
TIP_HOLE_DIA = 3.4    # 取付穴 ⌀（M3 ばか穴）
TIP_HOLE_PX = 14.0    # 取付穴のピッチ（X）
TIP_HOLE_PZ = 14.0    # 取付穴のピッチ（Z）

# ============================================================
# ベース（土台）
# ============================================================
# ⚠ 土台は「支柱を載せて、他の機構へねじ止めするための板」。自立させる前提を
#   やめたので、転倒を抑えるための張り出しとおもり入れは無くした。板の中心は
#   関節ではなく支柱に合わせる（関節中心だと +X 側に使わない板が 25mm 出る）。
#   幅と穴の位置は、支柱を覆い、穴が支柱の下に潜らない値まで自動で補正される
SHOULDER_Z = 60.0     # 板の上面から肩軸までの高さ
BASE_W = 44.0         # 土台 幅（X）… 支柱を覆う最小値まで自動で広がる
BASE_D = 52.0         # 土台 奥行き（Y）
BASE_T = 5.0          # 土台 厚み
BASE_FRONT = 26.0     # 肩軸から土台の前縁まで
BASE_HOLE_DIA = 4.5   # 取付穴 ⌀（M4 ばか穴）
BASE_HOLE_INSET = 7.0 # 前側 2 穴の縁からの距離
BASE_HOLE_REAR_Y = -19.0  # 後ろ 2 穴の位置（支柱の脇まで自動で下がる）
COLUMN_W = 30.0       # 支柱（土台〜肩）の幅（Y）
WIRE_SLOT_W = 12.0    # 肩サーボの配線を後ろへ抜く窓の幅（X）。0 で無し
WIRE_SLOT_H = 9.0     # 同 高さ（Z）

# 頬板（デッキ）の配線の通り道。線は尻尾側から出るので、差し込む途中でデッキ穴を
# 通る。溝が無いとリボンを噛んで入らない。深さはねじ下穴に当たる手前で止まる
WIRE_NOTCH_W = 6.0    # 溝の幅。0 で無し
WIRE_NOTCH_D = 2.5    # 本体面からの深さ（希望値）

# おもり入れ（後ろの箱）。高さ 0 で無し＝既定。
# ⚠ 単体で卓上に置くと、腕を前へ伸ばした時点で前へ倒れる。ねじ止めせずに
#   自立させたいときだけ BALLAST_H を戻す（M8 ナット 16 個・約 80g で釣り合う）
BALLAST_W = 64.0      # 内寸 幅（X）
BALLAST_D = 16.0      # 内寸 奥行き（Y）
BALLAST_H = 0.0       # 内寸 深さ（台板の上面から）。0 で箱なし
BALLAST_WALL = 2.5    # 箱の壁厚

# ============================================================
# 姿勢（プレビュー用。印刷用 STL は各パーツ単独の向きで書き出す）
# ============================================================
POSE_SHOULDER = 90.0  # 肩の角度（0=前へ水平, 90=真上）
POSE_ELBOW = -60.0    # 肘の角度（上腕からの相対角）
SHOW_SERVO = 1        # サーボとホーンのダミーを重ねる（1=表示）

# --- ブラウザUIからのパラメータ上書き ---
try:
    from param_override import apply_overrides
    apply_overrides(globals())
except Exception:
    pass
