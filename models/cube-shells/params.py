"""上方向にくりぬいた立方体シェル（入れ子ボックス）の寸法。単位: m"""

# くりぬく立方体（内側キャビティ）の一辺
CAVITY_SIZES = [
    0.175,   # 175mm
    0.160,   # 160mm
    0.110,   # 110mm
    0.072,   # 72mm
]

# 肉厚（底・側面とも共通）
# FDM の最小肉厚は 1.2mm（0.4mm ノズル×3 周）だが、
# 一辺 175mm の箱を 1.2mm で刷ると反り・座屈するため 2.0mm（5 周）を最小として採用
WALL = 0.002        # 2mm

# Boolean カッターの突き出し量（面一を避ける）
CUT_MARGIN = 0.0005  # 0.5mm

# セット表示（プレビュー用）で箱を並べるときの隙間
SET_GAP = 0.010      # 10mm
